require 'rails_helper'

RSpec.describe Pathway, type: :model do
  # it { is_expected.to have_field(:occurred).of_type(Integer) }
  # it { is_expected.to have_field(:title, :description).of_type(String) }

  # it { is_expected.to belong_to(:user) }
  # it { is_expected.to have_many(:milestones) }
  # it { is_expected.to have_many(:followers).as_inverse_of(:pathway).of_type(PathwayFollow) }

  # it { is_expected.to validate_presence_of(:user).on(:create) }

  
end